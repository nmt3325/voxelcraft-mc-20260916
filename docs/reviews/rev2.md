# Independent review rev2 — integration/mc-20260916

**Verdict: approve with followups.** Blockers 0, majors 6, minors 4.

- Reviewer: rev2 (independent; did not write any of this code)
- Branch reviewed: `integration/mc-20260916` @ `9c59c8ab15cc586f37412624ea1acc9f24dd2c24`, compared against contract sha `83c5553aacc1115c5c2e93a0622ceb685eb813ef`
- Scope: `packages/gameplay`, `packages/client`, `packages/assets-gen`, `apps/game`, `tests/e2e`, `tests/bench`
- Diff in scope: 130 files changed, +16277 / -45
- Every command below ran in the rev2 worktree on the gha runner (`linux-s4rptd3x`); job ids and exit codes are recorded.

Everything that is contractually pinned matches, and all six required gates pass. The majors are not broken behaviour in the shipped gameplay logic; they are gaps in what the green gates actually prove (the worker meshing path is never executed, the zero-console-error assertion cannot observe the failures the app emits, two Playwright specs skip instead of failing, and two of three bench budgets measure bench-local fixtures rather than shipped code), plus two real silent-failure paths in rendering and asset layer mapping.

## Findings

| id | severity | file and line | evidence | suggested fix |
| --- | --- | --- | --- | --- |
| R2-01 | major | `apps/game/src/main.ts:162-164`; `packages/client/src/worker/pool.ts:118-125,146`; `packages/client/src/__tests__/worker.test.ts` | The app builds the pool as `createMesherPool({ inline: testMode })`, with the comment "Workers are skipped under test so a worker load failure can never turn into a console error", so `pnpm test:e2e` (exit 0) never loads a worker. Every pool unit test passes `{ inline: true }`. `pnpm build` emits `mesher.worker-D1sygxe-.js` (10.71 kB) that no green gate executes. `transferablesOf` detaches `request.blocks/light/fluids` on `postMessage`; the inline path (pool.ts:133-140) does not, so a caller that reuses a padded section fails only in a browser. Currently safe by luck: `apps/game/src/world/localWorld.ts:249` allocates a fresh padded per request, and `tests/bench/src/bench.ts:122` reuses one padded but calls `meshSection` directly. | Add one Playwright spec that boots with workers enabled (e.g. `?test=1&workers=1`) asserting zero console errors and `chunks > 0`, plus a Node test driving `createMesherPool` against a stub `Worker` to cover `protocol.ts` and the transfer list. State the "buffers are detached" contract at every `pool.request` call site. |
| R2-02 | major | `tests/e2e/src/harness.ts:64-73`; `apps/game/src/main.ts:270,719`; `apps/game/src/assets.ts:12-16,19-30`; `packages/client/src/render/atlasTexture.ts:172` | `collectPageErrors` records only `console.error` and `pageerror`, but every real failure path logs `console.warn`: mesher errors, asset-load failure, atlas fallback. `loadSoundManifest` swallows a failed `sounds.json` fetch and returns `null`. The `__VC_HAS_ASSETS__` comment says the flag exists so missing assets are never requested, because a missing file "would surface as a console error and break the zero-error E2E assertion". So "app boots with zero console errors" holds even with the procedural fallback atlas and no sounds at all. | Assert the `voxelcraft.assetsReady` detail (`generated === true`, `sounds === true`) in `boot.spec.ts`, treat `console.warn` as a failure (with an explicit allowlist if needed), and assert `window.__vc.errors` is empty in the boot specs too, not only at `persistence.spec.ts:116`. |
| R2-03 | major | `tests/e2e/specs/boot.spec.ts:34-38`; `tests/e2e/specs/persistence.spec.ts:36-37`; `tests/e2e/src/harness.ts:76-91`; `apps/game/src/main.ts:692-694` | Both specs turn a missing app into a skip: `#ui-root` gets a 4 s wait then `test.skip`, and `waitForTestApi` returns `false` after an 8 s timeout which becomes `test.skip(MISSING_TEST_API)`. If `main()` throws before `window.__vc = api` (main.ts:697), the entire break/place/reload test is skipped and `playwright test` still exits 0. The stale messages still claim `apps/game` is "owned by client-a" and "not implemented yet". Secondary: the reload identity check compares `Number.parseInt(world.stateHash(), 16)`, which collapses values above 2^53, so two different worlds can compare equal. | Replace both `test.skip` guards with `expect(mounted).toBe(true)` / `expect(hasTestApi).toBe(true)`, delete the stale messages, and compare the hash as the original string rather than a lossy number. |
| R2-04 | major | `tests/bench/src/bench.ts:19-26,62-72,182-191`; `tests/bench/results/bench.json:12-23`; `tests/bench/package.json:14-16` | `chunkGenAvgMs` and `simTickAvgMs` measure `fixtures/terrain.ts` and `fixtures/tick.ts`, not shipped code. `bench.json` records `fixtureSubstitution.used: true` with the reason "packages/world, packages/sim and packages/gameplay are still stubs", which is untrue on this branch (all of `packages/gameplay` is added in this diff). The package declares `@voxelcraft/world`, `@voxelcraft/sim` and `@voxelcraft/gameplay` but imports none of them. The committed result is dated `2026-09-15T18:55:28Z`, before integration, and `pnpm bench` is not one of the required gates. Only `chunkMeshAvgMs` is real (`meta.mesher.source: "client"`, `fallbackUsed: false`). | Wire chunk generation and the sim tick to `@voxelcraft/world` / `@voxelcraft/sim` using the same `loadMesher` fallback pattern, regenerate `bench.json` on the integration sha, and keep `fixtureSubstitution` only for systems that are genuinely absent. |
| R2-05 | major | `packages/client/src/mesher/textures.ts:18-96,104-107`; `packages/assets-gen/src/texture-list.ts:6-80`; `packages/assets-gen/src/atlas.ts:70` | The 73-name texture list exists twice: `TEXTURE_NAMES` in the client and `REQUIRED_TEXTURES` in assets-gen. The atlas layer index is the position in the assets-gen list (`layerOf[name] = index`), while the mesher resolves layers from its own list. No file imports both (grep: `REQUIRED_TEXTURES` only inside assets-gen, `TEXTURE_NAMES` only inside client) and neither package depends on the other, so inserting or reordering one name silently re-maps every texture while all six gates stay green. The two lists are identical in order today. | Move the canonical ordered list into `@voxelcraft/core-types` and import it in both packages, or add a test that imports both lists and asserts deep equality. |
| R2-06 | major | `packages/client/src/mesher/greedy.ts:60,384-386,397-446,418-420`; `packages/client/src/__tests__/mesher.test.ts:105,183` | At `MAX_VERTICES = 65532` the mesher increments a local `droppedQuads` and clears the mask instead of emitting, producing holes with no runtime signal: `MeshResult.stats` is only `{ quads, meshMs }` and `droppedQuads` is reachable only through the debug-only `meshSectionWithDebug`. The cap is reachable: a section is 16^3 = 4096 cells and the cross pass emits 4 quads per cell, so a section full of cross blocks is 16384 quads = 65536 vertices. Tests only assert `droppedQuads === 0` on small fixtures. | Surface `droppedQuads` in `MeshStats` so the renderer can warn, and/or split a layer into multiple `MeshBuffer`s once the cap is hit so no geometry is discarded. |
| R2-07 | minor | `apps/game/package.json:16-20`; `apps/game/src/main.ts:513-518`; `apps/game/src/ui-bridge.ts:17-40` | `apps/game` declares `@voxelcraft/world`, `@voxelcraft/sim`, `@voxelcraft/gameplay` and `@voxelcraft/assets-gen`, but nothing under `apps/game/src` imports them (only `core-types` and `client`). The UI is wired to stubs: `onCraft` and `onMoveStack` are empty, and the hotbar is a hardcoded 9-entry list with `count: 64` instead of `createInventoryState`, so none of the 53 recipes, stack limits or durability rules is reachable from the app. Reported separately from the delegated `localWorld.ts` / `store.ts` swap. | Either drop the unused dependencies until they are wired, or keep them and add a smoke test that fails while the app has no gameplay integration, so the gap cannot drift unnoticed. |
| R2-08 | minor | `apps/game/src/main.ts:103-109,112-116`; `packages/client/src/mesher/appearance.ts` | Collision and crosshair targeting are derived from the renderer appearance table (`blocksMovement` returns `appearance.fullCube`; `targetable` returns `appearanceOf(id) !== null`), which is a client-local render double keyed by frozen BLOCK ids. Any block that is solid but not a full cube (slab, stair, door) is walk-through, and any block missing an appearance entry cannot be targeted. | Derive collision and targeting from the shared block definitions in `core-types`, and keep `appearanceOf` for rendering only. |
| R2-09 | minor | `packages/gameplay/src/crafting/furnace.ts:110-115,131-132` | `next.fuelTicks -= 1` runs before the `if (!canCook) continue` guard, so a furnace with a full output slot keeps burning fuel; with an unsmeltable input, `cookTicks` is reset to 0 while lit fuel continues to burn. This contradicts the intent described in the file's own doc comment. | Move the `fuelTicks` decrement below the `canCook` check (or decrement only while `canCook` is true). |
| R2-10 | minor | `packages/gameplay/src/redstone/engine.ts:160,278` | `dirty = field.visited >= cap && cap > 0` cannot distinguish a field that finished with `visited === cap` from one truncated by the budget at line 160, so a fully relaxed field stays dirty forever and the whole field is recomputed every tick. Correctness is unaffected; this is wasted work. | Return an explicit `truncated` flag from `computeField` and use it in `refreshField` instead of comparing `visited` to `cap`. |

## Verified green

All six required commands were run from `/home/runner/work/_temp/gha-mcp/linux-s4rptd3x/work/vc/wt/rev2` as one background job (`start_command` job `cda3bc0931a84f75`, log `/tmp/rev2-checks.log`, read back via job `b75108b115244e73`, exit 0; the log ends with the `=== ALL CHECKS COMPLETE ===` marker).

| command | exit code | notes |
| --- | --- | --- |
| `pnpm -r exec tsc --noEmit` | 0 | no diagnostics |
| `pnpm -r lint` | 0 | all workspaces clean |
| `pnpm -r test` | 0 | full unit suite across packages |
| `pnpm build` | 0 | `assets-gen: 73 textures, 19 sounds`; `atlas.png` 22345 bytes, 22 files written; vite 8.3.0, 58 modules, `index-BtogJwSC.js` 586.67 kB (gzip 152.79 kB), `mesher.worker-D1sygxe-.js` 10.71 kB |
| `pnpm size` | 0 | size report produced |
| `pnpm test:e2e` | 0 | Playwright `4 passed (9.4s)`: boot with zero console errors; test-mode boot at the E2E canvas size; HUD mounted under `#ui-root`; break, place, save and reload restore the same world state |

Supporting read-only jobs (all exit 0): `97d5c8d900d24d95` orientation, `07f5cd2c4b1b482e` file inventory, `e294234172ca484f` `git log --oneline 83c5553..HEAD` and `git diff --stat`, `0d0135d52e4e4cfc` crafting, `84a2951ce31c4518` core-types contract, `78c270ebb36e4105` inventory, `dfc5d56b3cd84700` block entities, `37d4a3da46d14f06` redstone, `f55ed1b801c04440` block and tool tables, `8660bc570d91454b` mesher, `86cd9902e7214bc4` renderer and worker, `16691e1e217c4025` appearance and client tests, `f95879e438df4f47` assets-gen, `1c6dfe6cd1184553` apps/game, `e8053eff4de147e6` tests/e2e, `d06c5d9be4ef4a72` tests/bench and `perf.ts`, `ec0e1ca40695420c` gameplay persistence, `4f26684390b34910` and `e3b335ca4977447c` targeted greps, `06e229e95182482b` padded allocation check.

### Contract values checked against the source

- Inventory 36 slots = 9 hotbar + 27 main; craft grids 4 and 9; chest 27; furnace 3 — `packages/core-types/src/items.ts`, asserted in `packages/core-types/src/__tests__/contract.test.ts:33-37`.
- Tool tiers 0-4 with `TIER_SPEED = [1, 2, 4, 6, 8]`; block ids 0-63 (`LAVA_FLOWING` 63); item ids 256-304 (`SHEARS` 304) — `packages/core-types/src/blocks.ts`, `items.ts`.
- Recipes: 53 = 25 crafting + 16 tool (4 classes x 4 tiers) + 12 smelting, `RECIPE_DEF_COUNT = RECIPE_DEFS.length` — `packages/gameplay/src/crafting/recipes.ts:121-184`. Shaped matching is bounding-box shift invariant (`registry.ts:43-110`) and shaped is tried before shapeless (`registry.ts:162-188`); mirrored shapes are not matched, which is consistent with the contract wording ("shift-invariant") but not with a mirror-tolerant reading, so it is called out here rather than filed as a defect.
- Redstone max power 15, `maxUpdatesPerTick` 2048, button 20 ticks, piston 2 — `core-types/src/redstone.ts`; deterministic (y, z, x) relaxation in `gameplay/src/redstone/engine.ts`.
- Save version 1; `PERSIST.dbName = "voxelcraft"` applied as the default at `packages/gameplay/src/persistence/indexedDbStore.ts:132-133`; chunk store keyPath `["worldId", "cx", "cz"]` at `indexedDbStore.ts:90`, with negative-coordinate ordering covered by `indexedDbStore.test.ts:38-42`.
- Mesher padded size 18, padded volume 5832, vertex stride 8 u16 — `core-types/src/mesh.ts`, asserted at `contract.test.ts:90-100`; strides match `paddedIndex` in `packages/client/src/mesher/padded.ts`.
- Render layers opaque 0, cutout 1, translucent 2 — `core-types/src/blocks.ts`; GLSL3 output is declared explicitly (`layout(location = 0) out vec4 fragColor;`, `packages/client/src/render/material.ts:88`, `glslVersion: THREE.GLSL3` at :149), cutout uses `discard`, translucent uses alpha 0.78 with `depthWrite: false`, and draw order follows `renderOrder = buffer.layer`.
- E2E render distance 2 and canvas 640x360 — `packages/core-types/src/perf.ts:22-24`, pinned by `tests/e2e/unit/harness.test.ts:13-17` and asserted live at `persistence.spec.ts:49`.
- Bench budgets chunkGen 6 ms, chunkMesh 12 ms, simTick 8 ms, `failFactor: 3` — `packages/core-types/src/perf.ts:31-37`, applied in `tests/bench/src/bench.ts:204-215`. See R2-04 for what those numbers actually measure.
- Assets are fully procedural: 73 generators and 19 sounds, manifest `sampleRate` = `SOUND_SAMPLE_RATE` = 22050 (`packages/assets-gen/src/wav.ts:3`, `sounds.ts:118`, asserted in `__tests__/sounds.test.ts:30`), regeneration is seeded by an FNV-1a asset seed with a determinism test, and no runtime network fetch exists in the generator. `git ls-files` filtered to binary asset extensions returns nothing, so no third-party binaries are committed.
- The E2E persistence loop is real: it finds a solid block under spawn, breaks it (asserts the voxel reads 0), places a different id (asserts the id), saves, reloads the page, then asserts the placed id, the world hash, the world id and the seed all survive (`tests/e2e/specs/persistence.spec.ts:53-119`).

## Not verified

- The worker meshing path was never executed (see R2-01), so `mesher.worker.ts`, `protocol.ts` and the `postMessage` transfer list are unverified at runtime.
- `pnpm bench` was not run; the bench findings are from source plus the committed `results/bench.json`.
- `packages/core-types`, `packages/world` and `packages/sim` are rev1's scope and were read only where the contract or my scope touches them. `apps/game/src/world/localWorld.ts` and `apps/game/src/world/store.ts` were treated as sanctioned placeholders and are not filed as findings.
- IndexedDB persistence was checked through `fake-indexeddb` unit tests only; no real-browser storage run.
